<div>
    {{#with service}}
    <div class="alert bg-success text-light text-center py-3">
        <div>
            <h2>{{title}}</h2>
            <p class="my-3">{{description}}</p>
        </div>
        <div>
            {{#each buttons}}
                <button class="btn btn-primary ms-2" href="{{link}}">{{text}}</button>
            {{/each}}
        </div>
    </div>
    {{/with}}
</div>